opencv-python>=4.9
ultralytics>=8.2
hsemotion-onnx>=0.3
onnxruntime>=1.17          # CPU. For GPU, uninstall and install ONE of:
                           #   onnxruntime-directml  (Windows, AMD/any GPU)
                           #   onnxruntime-rocm      (Linux, AMD GPU)
                           #   onnxruntime-gpu       (NVIDIA CUDA)
pandas>=2.2
pyarrow>=15
scikit-learn>=1.4
pytest>=8.0
numpy>=1.26
